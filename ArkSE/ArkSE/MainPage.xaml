<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:arkSe="clr-namespace:ArkSE;assembly=ArkSE"
             x:Class="ArkSE.MainPage"
             x:Name ="MainPage0">

    <StackLayout BindingContext="{Binding Source={x:Reference MainPage0}, Path=MainViewModel}">
        <Entry TextChanged="InputView_OnTextChanged"></Entry>
        <ListView ItemsSource="{Binding OfficialServers}"
                  ItemSelected="ListView_OnItemSelected">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid>
                            <Label  Grid.Column="0" Grid.Row="0" Text="{Binding Name}" TextColor="Black" FontAttributes="Bold" IsVisible="{Binding Visible}"/>
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>
